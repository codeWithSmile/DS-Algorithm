class nodes {
    constructor(values) {
        this.values = values;
        this.next = null;
    }
}
class LinkedList {
    constructor() {
        this.head = head;
        this.size = 0;
    }
    isEmpty() {
        return this.size === 0;
    }
    getSize() {
        return this.size;
    }
}

const list = new LinkedList();
console.log('List is empty', list.isEmpty());
console.log('List size', list.getSize());
